## How it works

A small CPU with a pin-oriented, cycle-exact instruction set. Protocols (UART, SPI, I2C) are implemented as firmware loaded over the input pins. Work in progress.

## How to test

Load a program over ui_in, assert run, and observe the protocol on the uio pins. Work in progress.

## External hardware

None yet.